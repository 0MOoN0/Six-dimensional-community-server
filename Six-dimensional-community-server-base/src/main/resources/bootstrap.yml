server:
  port: 9001
spring:
  application:
    name: sdcommunity-base
  jpa:
    database: mysql
    show-sql: true
  cloud:
    config:
      name: base
      profile: dev
      label: master
      uri: http://127.0.0.1:12000
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/