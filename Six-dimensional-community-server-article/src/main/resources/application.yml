server: 
  port: 9004
spring:
  application:  
    name: sdcommunity-article #指定服务名
  jpa:
    database: mysql
    show-sql: true
  profiles:
    active: dev
jwt:
  config:
    key: sdcommunity
    ttl: 604800000 # 一周过期
eureka:
  instance:
    prefer-ip-address: true
